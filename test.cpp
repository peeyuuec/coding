#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>

using namespace std;

#define lll long
int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */  
    int n,
    lll k;
    cin>>n>>k;
    lll a[n];
    for(ll i=0;i<n;i++){
        cin>>a[i];
    }
    sort(a,a+n);
    return 0;
}

